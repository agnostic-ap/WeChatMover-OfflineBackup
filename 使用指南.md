# 微信离线备份 · 使用指南

把 Mac 上微信的全部数据（聊天记录、图片视频、文件）完整打包备份到移动硬盘；需要时再按计划恢复。备份完成后硬盘随时可以拔下，平时用微信不需要插盘。本指南面向普通用户，照着点就行。

---

## 一、使用前必读（30 秒）

1. **备份/恢复前会自动完全退出微信**（工具替你做，先礼后兵：先正常退出，几秒不动再强制结束）。
2. **移动硬盘什么格式都行**：APFS、exFAT、NTFS（可读写时）均可。备份以 ZIP 归档形式存放，macOS 专有的文件元数据也会完整保留——exFAT 盘同样安全。
3. **恢复的最可靠场景**：同一台 Mac、同一个系统用户、同版本微信、同一个微信账号。**换 Mac 恢复属于实验性质**，微信可能要求重新登录，不保证全部成功。
4. **请与官方备份并存**：本工具不能替代微信自带的「聊天记录备份与迁移」。重要聊天记录建议两条路都留。
5. 恢复时你现在的微信数据**只会改名保留、绝不删除**，出错自动回滚；万一连自动回滚都没能完全成功，工具会明确报错并列出你的原数据所在的具体目录（依然完好），绝不会假装成功。确认一切正常后再手动清理。
6. 备份/恢复的每个写入步骤前，工具都会再次确认微信确实没有在运行（防止刚退出又被打开）；备份位置也不允许选在微信数据目录里面或外面一层。
7. 本工具**不修改微信程序本身**，不需要重新签名微信，微信更新也不受影响。

## 二、安装

1. 把 `WeChatMover.app` 拖进「应用程序」文件夹。
2. 首次双击如提示「无法验证开发者」：右键 App → 打开 → 再点「打开」（只需一次）。
3. 首次使用如读不到微信数据，请到 系统设置 → 隐私与安全性 → **完全磁盘访问**，打开 WeChatMover 的开关后重启本工具。

## 三、备份（核心操作）

1. 插上移动硬盘，打开本工具。
2. 点「**选择备份位置…**」，选硬盘上的任意文件夹（备份会写进其中的 `WeChatBackups` 目录）。
3. 界面会列出将要备份的微信数据目录和大小；如需保留当前版本微信安装包，勾选「同时归档微信应用本体」。
4. 点「**开始备份…**」→ 确认。工具自动退出微信后开始打包，进度条实时显示。**期间不要打开微信、不要拔硬盘。**
5. 完成后会提示快照名称与大小，此时**硬盘即可安全推出、拔下**。

每次备份生成一个带时间戳的独立快照（如 `WeChatBackup-20260830-153000`），互不影响；老快照留几份更保险，不需要时在列表中删除。

## 四、快照管理

- **列表**：连接硬盘并刷新后，全部快照按时间倒序显示：时间、大小、当时的微信版本、完整性徽标。「未完成/损坏」的快照（如备份中途拔盘留下的）会明确标出，不能用于恢复。
- **详情**：查看快照里每个归档的文件数、大小和 SHA-256 校验值。
- **验证**：逐个归档重新计算 SHA-256 并与清单核对，几分钟内告诉你这份备份是否完好。**建议每次备份完顺手验证一次。**
- **删除**：删除某个快照（有确认；只影响硬盘上的这份备份，不动 Mac 上的微信）。

## 五、恢复

1. 连接硬盘，在快照列表中选中要恢复的快照，点「**恢复…**」。
2. 工具先只给你看一份**恢复计划**：每项数据恢复到哪里、现有数据如何改名保留、空间够不够、微信版本是否一致。**这一步不做任何改动。**
   - 如当前微信版本与快照不一致，需勾选「我已了解风险」才能继续；最稳妥的做法是先装回同版本微信（若备份时勾了「归档应用本体」，快照里就有 `app-WeChat.zip`，手动解压拖回「应用程序」即可——工具不会自动替你覆盖）。
3. 点「继续」后还有**最终确认**。确认后工具自动退出微信，先校验每个 ZIP 的完整性（SHA-256）和路径安全，全部通过才动手。
4. 恢复时你现有的微信数据会改名为 `xxx.wcm-rollback-时间戳` 保留在原位；任何一步失败自动回滚到恢复前的状态。
5. 完成后打开微信确认一切正常，再手动删除那些 `.wcm-rollback-*` 目录释放空间。

## 六、常见问题

**备份期间能用电脑吗？** 能，但别打开微信、别拔硬盘。

**平时用微信要插盘吗？** 不用。备份是独立副本，微信照常从内置盘运行。

**备份盘丢了/坏了会影响微信吗？** 不会，Mac 上的微信数据一直在原位。

**换了新 Mac 怎么恢复？** 新 Mac 上先装好同版本微信（最好登录一次再完全退出），然后用本工具恢复快照。属实验性质，请做好重新登录的准备。

**快照能自己拷贝/异地多存一份吗？** 可以。快照目录是自包含的（若干 ZIP + 清单 + 完成标记），整个目录复制到别处同样有效。
