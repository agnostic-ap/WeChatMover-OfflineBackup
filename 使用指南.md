# WeChatMover 使用指南

把 Mac 微信（官网版）几十上百 GB 的数据搬到外置硬盘，内置盘空间瞬间释放，微信使用完全无感。本指南面向普通用户，照着点就行。

---

## 一、使用前必读（30 秒）

1. **只支持微信官网版**：必须是从 [微信官网](https://weixin.qq.com/) 下载 DMG 安装的微信。App Store 版不支持（工具会自动识别并提示）。
2. **外置硬盘建议 APFS 格式**。exFAT / NTFS 也能用，但可能出现空间膨胀、性能下降，工具会有醒目警告，继续与否由你决定。
3. **迁移后，用微信时必须插着这块外置硬盘**。没插盘时打开微信，微信会在原位建空目录（工具会红色警示，插上盘重启微信即可恢复）。
4. 工具全程"先复制、校验通过、再处理原数据"，任何一步失败自动回滚，不会丢数据。但重要聊天记录仍建议另有备份。

## 二、安装

1. 把 `WeChatMover.app` 拖进「应用程序」文件夹。
2. 首次双击如果提示"无法验证开发者"：在 App 上**右键 → 打开**，再点「打开」即可（或去 系统设置 → 隐私与安全性，点「仍要打开」）。只需操作一次。

## 三、两项一次性授权

工具首次使用会引导你授权，都只需一次：

| 权限 | 干什么的 | 怎么开 |
| --- | --- | --- |
| **完全磁盘访问** | 读取微信容器里的数据 | 按工具内按钮跳到系统设置，打开 WeChatMover 开关，然后**重启本工具** |
| **App 管理** | 迁移后给微信重签名 | 按工具内弹窗指引打开开关；列表里没有就点「+」手动添加 |

都不需要输入密码。

## 四、迁移到外置硬盘（核心操作）

1. 插上外置硬盘，打开 WeChatMover。
2. 确认三张卡片状态正常：识别到微信版本、来源是「官网版」、安全检查「全部通过」。
3. 点「目标位置」行的 **「更改…」**，在外置硬盘上选（或新建）一个文件夹。
4. 点 **「迁移到外置硬盘」**，确认框里选「退出微信并开始迁移」——微信会自动退出，无需手动操作。
5. 等待拷贝（几十 GB 可能要几分钟到十几分钟，有真实进度条）。完成后工具自动给微信重签名并复核。
6. 打开微信确认聊天记录、文件都正常。
7. **确认没问题后**，回到工具点 **「清理备份…」**，这一步才真正释放内置盘空间。

> 迁移后首次用微信，截图/语音可能提示要权限：按工具内「权限重新授权指南」，在系统设置里**先把微信移除（选中点「−」），再点「+」重新添加**，比重开开关可靠。

## 五、微信打不开了怎么办（更新后常见问题）

**微信更新** 或 **macOS 系统更新** 后，微信可能打不开（更新会恢复官方签名，与迁移后的数据结构冲突）。

解决办法：打开 WeChatMover → 点开「安全检查」卡片 → 点 **「重新签名微信」**，几秒完成，微信即可正常打开。

- 工具能识别"官方签名恢复"的情况并主动提示「需重新签名」；
- 但有些情况（如系统更新）检测不出来，所以这个按钮**一直放在那里**，打不开就点它，点坏了也不亏——重签名本身对微信无害。

## 六、各个按钮都是干什么的

| 按钮 | 作用 | 什么时候用 |
| --- | --- | --- |
| **迁移到外置硬盘** | 把微信数据搬到外置盘 | 首次使用 |
| **更改…**（目标位置行） | 已迁移时更换目标位置，两种模式：**转移**（完整拷贝到新位置并清除原位置数据）或**不转移**（不拷贝：新位置已有数据时直接改指过去，旧数据保留；或仅更新记录） | 换硬盘/换文件夹时 |
| **还原外置存储数据到 Mac…** | 把外置盘上的数据搬回内置盘 | 不想用外置盘了 |
| **还原内置存储数据到 Mac…** | 用内置盘上的 `_backup` 备份秒还原（不插外置盘也能用，外置数据保留不动） | 放弃迁移、回到旧数据 |
| **用外置数据覆盖内置…** | 内置数据已被还原后，发现外置的数据更新，用外置的覆盖回来 | 还原错了想反悔 |
| **清理备份…** | 删除内置盘上的 `_backup` 备份，**真正释放空间** | 迁移后确认微信正常 |
| **清理外置数据…** | 删除外置盘上的微信数据 | 仅还原回 Mac 后可点（防止误删） |

拿不准"内置备份和外置数据哪个新"？不用管——还原时工具会自动比对指纹，该提示时会弹窗告诉你哪边更新、建议用哪个。

## 七、常见问题

**Q：迁移时提示"目标位置已有数据"？**
多半是上次迁移中断或重复操作。弹窗里三个选择：
- 「直接使用外置数据」：外置数据完整时最省事，不拷贝，本地数据转为备份直接软链过去；
- 「删除旧数据并重新迁移」：旧数据确认没用再选；
- 「取消」：自己先去看看。

**Q：迁移/转移中途硬盘掉了？**
不会丢数据。所有操作都是"先复制、校验通过、最后才删原数据"。重新插盘后再来一次即可，已完成的项会自动跳过（断点续传）。

**Q：重签名提示 Operation not permitted？**
缺少「App 管理」权限，按工具弹出的指引授权后点「重试重签名」。兜底：弹窗里有终端命令可复制，在「终端」里执行。

**Q：微信版本更新后要重新迁移吗？**
不用。数据还在外置盘原位，只需点一次「重新签名微信」（见第五节）。

**Q：App Store 版微信能用吗？**
不能。卸载后去微信官网下载 DMG 版安装，聊天记录在服务器和原数据里，重装登录后按需恢复。

---

遇到问题可查看工具底部的「运行日志」（可导出），或到 GitHub 仓库提 Issue。
